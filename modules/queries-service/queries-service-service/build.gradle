dependencies {
	compile project(":modules:queries-service:queries-service-api")

	compileOnly group: "com.liferay.portal", name: "release.dxp.api"
}

buildService {
	apiDir = "../queries-service-api/src/main/java"
}

group = "queries.service"